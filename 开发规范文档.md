# 开发规范文档 - Vue/Nuxt 管理后台

**最后更新**: 2026-05-19
**Node.js**: >= 22.x | **pnpm**: >= 9.x

---

## 一、技术栈

| 类别      | 技术                            | 版本           |
| --------- | ------------------------------- | -------------- |
| 元框架    | Nuxt                            | 4.4.2          |
| UI 组件库 | Nuxt UI                         | 4.6.0          |
| 样式      | Tailwind CSS 4.x + Nuxt UI 主题 | 内置于 Nuxt UI |
| 状态管理  | Pinia                           | 3.0.4          |
| 类型安全  | TypeScript 5.3.3 + Zod          | 3.22.4         |
| 图标      | @nuxt/icon + Heroicons/Lucide   | -              |

---

## 二、项目结构

```
pages/                    # 页面路由（只做组装，无业务逻辑）
layouts/                  # 布局组件
components/               # 全局共享组件（自动导入）
composables/              # 组合式函数（自动导入）
stores/                   # Pinia store（自动导入）
modules/                  # 业务模块内聚
server/api/               # Nitro API 路由
server/utils/             # 服务端工具
middleware/               # 路由中间件
utils/                    # 纯工具函数
assets/css/               # 全局样式
types/                    # 全局类型定义
```

---

## 三、编码规范

### 命名约定

- **组件名**: PascalCase（`LoginForm.vue`）
- **组合式函数**: `useXxx`（`useMenu.ts`）
- **Pinia store**: `useXxxStore`（`useUserStore`）
- **目录名**: kebab-case（`user-profile/`）
- **API 路由**: `[name].[method].ts`（`login.post.ts`）

### Vue SFC 结构顺序

```vue
<script setup lang="ts">
// 1. 类型导入
// 2. defineProps/defineEmits
// 3. 组合式函数
// 4. 响应式数据
// 5. 计算属性
// 6. 方法/事件处理
// 7. 生命周期钩子
</script>
```

### 自动导入规则

- `components/`、`composables/`、`utils/`、`stores/` 下的文件**禁止手动 import**
- Nuxt UI 组件和图标完全自动导入

---

## 四、状态管理（Pinia）

使用 `defineStore` + Composition API 风格：

```ts
// stores/user.store.ts
export const useUserStore = defineStore("user", () => {
  const user = ref<User | null>(null);

  async function fetchUserInfo() {
    const data = await $fetch("/api/auth/me");
    user.value = data.user;
  }

  return { user, fetchUserInfo };
});
```

---

## 五、API 规范

### 客户端数据获取

- 页面级数据：`useFetch`
- 交互请求：`$fetch`

### 服务端 API 路由

文件命名：`[name].[method].ts`

| 文件              | 方法   | 路径                   |
| ----------------- | ------ | ---------------------- |
| `index.get.ts`    | GET    | /api/[module] 列表     |
| `[id].get.ts`     | GET    | /api/[module]/:id 详情 |
| `index.post.ts`   | POST   | /api/[module] 创建     |
| `index.put.ts`    | PUT    | /api/[module] 更新     |
| `index.delete.ts` | DELETE | /api/[module] 删除     |

### 统一响应格式

```ts
interface ApiResponse<T = any> {
  code: number; // 20000 表示成功
  data: T;
  message: string;
  success: boolean;
  total?: number; // 列表接口
}
```

---

## 六、组件规范

### 图标使用

```vue
<UIcon name="i-heroicons-home" />
<UIcon :name="`i-heroicons-${iconName}`" />
```

### 表单验证

- 简单表单：手动校验 + `UInput` 的 `error` 属性
- 复杂表单：使用 `zod` 定义 schema

### 表格

- 简单列表：`UTable`
- 复杂功能：使用 Nuxt UI 表格组件或自定义实现

---

## 七、安全规范

- **禁止 `v-html`**：除非内容经过 DOMPurify 清洗
- **用户输入**：服务端必须使用 Zod 校验
- **Cookie**：设置 `httpOnly`、`secure`、`sameSite: 'strict'`

---

## 八、代码质量

- ESLint：`pnpm lint`
- 类型检查：`pnpm type-check`
- 生产构建：自动移除 `console.log` 和 `debugger`

---

**后续所有代码生成请求均基于以上规范执行。**
